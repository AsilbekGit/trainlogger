name: train_logger
description: Train movement GPS logger with live map and CSV export.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: '>=3.16.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # State management
  flutter_riverpod: ^2.5.1

  # Location / GPS
  geolocator: ^12.0.0
  permission_handler: ^11.3.1

  # Map
  flutter_map: ^6.1.0
  latlong2: ^0.9.1

  # CSV export / share
  share_plus: ^9.0.0
  path_provider: ^2.1.4
  csv: ^6.0.0

  # Misc
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true